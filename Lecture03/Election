import java.util.Scanner;

public class Main{
    public static void main(String[] args){
        int age = 0;
        Scanner input = new Scanner(System.in);
        System.out.println("Please enter your age: ");
        age = input.nextInt();
        if(age>=18){
            System.out.println("cast a vote");
        }else{
            System.out.println("sorry, you're too young");
        }
    }
}
>>
Please enter your age: 
17
sorry, you're too young

-------From lecturer--------
// a simple java program to determine whether a given candidate is a qualified voter
// input: age
// output: show the message (on screen)
import java.util.scanner;
public class Election{
    public static void main(String[] args){

        //declare the variable
        int age;

        Scanner input = new Scanner(System.in);
        age input.nextInt();

        if (age>= 18)
            // if the condition is true, it's a qualified voter
            System.out.println("cast a vote");
        else
            System.out.println("sorry, you're too young");
    }//end the program
}

/* Trial values in...
i. Normal zone (valid)
ii. Outside normal zone (invalid)
iii. boundary value: +-1

>=80: 18(normal); 8, 80 (outside normal); 17, 19 (boundary)
 */
